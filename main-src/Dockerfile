FROM node:latest

WORKDIR /app

COPY package.json ./

RUN npm install

COPY . .

EXPOSE 3000

ARG MONGODB_URL
ARG VITE_RECIPE_KEY
ARG VITE_RAPID_API_KEY

ENV MONGODB_URL=$MONGODB_URL
ENV VITE_RECIPE_KEY=$VITE_RECIPE_KEY
ENV VITE_RAPID_API_KEY=$VITE_RAPID_API_KEY

CMD ["npm", "run", "dev"]